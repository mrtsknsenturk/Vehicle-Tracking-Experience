kind = "web"
previewPath = "/"
title = "Vehicle Tracking Mascot Experience"
version = "1.0.0"
id = "artifacts/mascot-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24693

[services.development]
run = "pnpm --filter @workspace/mascot-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mascot-app", "run", "build" ]
publicDir = "artifacts/mascot-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24693"
BASE_PATH = "/"
