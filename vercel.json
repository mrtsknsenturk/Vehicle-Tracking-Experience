{
  "buildCommand": "pnpm --filter @workspace/mascot-app run build && mkdir -p public && cp -r artifacts/mascot-app/dist/public/* public/",
  "installCommand": "pnpm install --prod=false",
  "outputDirectory": "public",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
