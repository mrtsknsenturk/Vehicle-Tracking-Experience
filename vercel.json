{
  "buildCommand": "cd /vercel/path0 && pnpm --filter @workspace/mascot-app run build && node /vercel/path0/scripts/copy-build-output.js",
  "installCommand": "pnpm install --prod=false",
  "outputDirectory": "public",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
